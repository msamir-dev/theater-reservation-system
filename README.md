# نظام حجز مقاعد المسرح

نظام حجز مقاعد المسرح هو تطبيق ويب متكامل يتيح للمستخدمين حجز مقاعدهم في المسرح بسهولة ويمنح المديرين أدوات قوية لإدارة الحجوزات.

## المميزات

- ✅ واجهة مستخدم سهلة الاستخدام لعرض المقاعد المتاحة
- ✅ نظام حجز مباشر مع إشعار فوري للمدير
- ✅ لوحة تحكم إدارية متكاملة
- ✅ إرسال تذاكر رقمية عبر واتساب مع رموز QR
- ✅ تصنيف المقاعد (VIP وعادي)
- ✅ نظام تسجيل دخول آمن للمديرين
- ✅ قاعدة بيانات محلية (SQLite) للتطوير
- ✅ جاهز للنشر على المنصات السحابية (PostgreSQL)

## المتطلبات

- Python 3.7 أو أحدث
- pip (مدير حزم Python)

## 🚀 التشغيل

### التشغيل المحلي

1. **استنساخ المشروع أو تنزيل الملفات**
```bash
git clone <repository-url>
cd المسرحية
```

2. **إنشاء بيئة افتراضية (اختياري لكنه مستحسن)**
```bash
python -m venv venv
source venv/bin/activate  # على Linux/Mac
# أو
venv\Scripts\activate  # على Windows
```

3. **تثبيت المتطلبات**
```bash
pip install -r requirements.txt
```

4. **تهيئة قاعدة البيانات**
```bash
python init_db.py
```

5. **تشغيل التطبيق**
```bash
python app.py
```

6. **فتح المتصفح**
افتح المتصفح واذهب إلى: `http://localhost:5000`

### النشر على الإنترنت

راجع قسم "النشر على الإنترنت" أدناه للحصول على تعليمات النشر الكاملة على:
- Heroku
- Render
- PythonAnywhere
- خوادم VPS

## بيانات تسجيل الدخول الافتراضية

- **البريد الإلكتروني:** vipwinni@shubra.com
- **كلمة المرور:** vipwinni123@

⚠️ **مهم:** قم بتغيير كلمة المرور الافتراضية بعد أول تسجيل دخول!

## هيكل المشروع

```
المسرحية/
├── app.py              # ملف التطبيق الرئيسي
├── init_db.py          # تهيئة قاعدة البيانات
├── requirements.txt    # متطلبات Python
├── models/             # نماذج قاعدة البيانات
├── routes/             # مسارات التطبيق
├── static/             # ملفات CSS، JavaScript، الصور
├── templates/          # قوالب HTML
├── whatsapp_service.py # خدمة إشعارات واتساب
└── README.md          # هذا الملف
```

## النشر على الإنترنت

### على منصة Render

1. أنشئ حساباً على [Render](https://render.com)
2. أنشئ تطبيق Web Service جديد
3. ربط حساب GitHub الخاص بك
4. اختر المستودع الذي يحتوي على هذا المشروع
5. اضبط الإعدادات:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn app:app`
   - **Environment:** Python 3.9+
6. أضف متغيرات البيئة:
   ```
   DATABASE_URL=postgresql://username:password@host:port/database
   SECRET_KEY=your-secret-key-here
   FLASK_ENV=production
   ```

### على منصة Heroku

1. أنشئ حساباً على [Heroku](https://heroku.com)
2. ثبّت Heroku CLI على جهازك
3. أنشئ تطبيقاً جديداً:
```bash
heroku create your-app-name
```
4. أضف إضافة PostgreSQL:
```bash
heroku addons:create heroku-postgresql:hobby-dev
```
5. دفع الكود إلى Heroku:
```bash
git push heroku main
```

## إعداد خدمة واتساب

لتفعيل إرسال الإشعارات عبر واتساب:

1. سجّل في إحدى خدمات واتساب API (مثل Twilio أو MessageBird)
2. أضف بيانات الاعتماد إلى متغيرات البيئة:
   ```
   WHATSAPP_API_KEY=your-api-key
   WHATSAPP_API_URL=https://api.your-provider.com/v1/messages
   WHATSAPP_ENABLED=true
   ```

## التخصيص

### تغيير عدد المقاعد أو الترتيب

عدّل ملف `init_db.py` لتغيير:
- عدد الصفوف
- عدد المقاعد في كل صف
- تصنيف المقاعد (VIP/عادي)
- ترتيب المقاعد (يمين/يسار)

### تغيير مظهر الموقع

- عدّل ملف `static/css/style.css` لتغيير الألوان والتصميم
- عدّل ملف `templates/index.html` لتغيير هيكل الصفحة الرئيسية
- عدّل ملف `static/js/main.js` لتغيير سلوك واجهة المستخدم

## دعم فني

إذا واجهتك مشاكل أثناء التثبيت أو الاستخدام:

1. تأكد من تثبيت جميع المتطلبات بشكل صحيح
2. تحقق من أن قاعدة البيانات تم تهيئتها بشكل صحيح
3. تأكد من متغيرات البيئة المطلوبة
4. راجع سجلات الأخطاء في وحدة التحكم

## المساهمة

نرحب بالمساهمات لتحسين هذا المشروع! يمكنك:

- الإبلاغ عن الأخطاء
- اقتراح ميزات جديدة
- تحسين التصميم
- تحسين الأداء

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

تم تطوير هذا المشروع باستخدام Flask وHTML/CSS/JavaScript مع حب كبير للمسرح! 🎭